# 📋 Simple Deployment Steps

## Quick Reference

```bash
# 1. Go to worker directory
cd worker

# 2. Login to Cloudflare (opens browser)
wrangler login

# 3. Deploy
wrangler deploy

# 4. Test (replace URL with your worker URL)
curl https://your-worker-name.your-subdomain.workers.dev/health
```

That's it! 🎉


